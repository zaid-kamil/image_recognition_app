import 'package:flutter/material.dart';

class ImageSelectionWeb extends StatelessWidget {
  const ImageSelectionWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          width: 500,
          height: 500,
          color: Colors.red,
        ),
      ),
    );
  }
}
